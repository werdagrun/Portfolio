

const MenuItem = ({name}) => {
    return (
        <div >
            < input class='menuItem' type='Submit' value= {name} className='btn btn-block'/> 
        </div>
    )
}
export default MenuItem